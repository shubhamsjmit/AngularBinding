import { DataChangeDirective } from './data-change.directive';

describe('DataChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new DataChangeDirective();
    expect(directive).toBeTruthy();
  });
});
